import { o as Button, r as getUser } from "./use-auth-D1Km7QHf.js";
import { t as Card } from "./card-Cm76wao2.js";
import { t as Label } from "./label-DjH-8qU2.js";
import { t as DashboardShell } from "./dashboard-shell-COSsbC9W.js";
import { t as Badge } from "./badge-DtdLsTq1.js";
import { i as updateBookingStatus, r as getWorkerBookings } from "./booking-CVT-s6cz.js";
import { n as getWorkerByUserId } from "./worker-CJ7ieySA.js";
import { t as Switch } from "./switch-Db4RmlUk.js";
import { useEffect, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { CalendarCheck, DollarSign, LayoutDashboard, MessageSquare, Star, ToggleLeft } from "lucide-react";
//#region src/routes/dashboard.worker.tsx?tsr-split=component
var items = [
	{
		to: "/dashboard/worker",
		label: "Overview",
		icon: LayoutDashboard
	},
	{
		to: "/messages",
		label: "Messages",
		icon: MessageSquare
	},
	{
		to: "/dashboard/worker",
		label: "Availability",
		icon: ToggleLeft
	}
];
function WorkerDashboard() {
	const [available, setAvailable] = useState(true);
	const [requests, setRequests] = useState([]);
	const [loading, setLoading] = useState(true);
	useEffect(() => {
		loadDashboard();
	}, []);
	async function loadDashboard() {
		try {
			const user = getUser();
			if (!user) return;
			const worker = await getWorkerByUserId(Number(user.id));
			setAvailable(worker.available);
			const bookings = await getWorkerBookings(worker.id);
			setRequests(bookings);
		} catch (err) {
			console.error(err);
			toast.error("Failed to load dashboard");
		} finally {
			setLoading(false);
		}
	}
	async function decide(bookingId, status) {
		try {
			await updateBookingStatus(bookingId, status);
			setRequests((prev) => prev.map((b) => b.id === bookingId ? {
				...b,
				status
			} : b));
			toast.success(status === "ACCEPTED" ? "Booking accepted" : "Booking declined");
		} catch {
			toast.error("Failed to update booking");
		}
	}
	const today = requests.filter((b) => b.status === "ACCEPTED" || b.status === "IN_PROGRESS");
	const pending = requests.filter((b) => b.status === "PENDING");
	const completed = requests.filter((b) => b.status === "COMPLETED");
	const earnings = completed.reduce((sum, b) => sum + b.price, 0);
	return /* @__PURE__ */ jsxs(DashboardShell, {
		title: "Worker Dashboard",
		role: "worker",
		items,
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-4 sm:grid-cols-4",
				children: [
					/* @__PURE__ */ jsx(StatCard, {
						label: "Today",
						value: String(today.length),
						icon: CalendarCheck
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Pending",
						value: String(pending.length),
						icon: CalendarCheck
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Completed",
						value: String(completed.length),
						icon: Star
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Earnings",
						value: `₹${earnings}`,
						icon: DollarSign
					})
				]
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "mt-6 flex items-center justify-between p-5",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "avail",
					className: "text-base font-semibold",
					children: "Available for bookings"
				}), /* @__PURE__ */ jsx("p", {
					className: "text-xs text-muted-foreground",
					children: "Toggle off to stop receiving requests."
				})] }), /* @__PURE__ */ jsx(Switch, {
					id: "avail",
					checked: available,
					onCheckedChange: setAvailable
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "mt-8 grid gap-6 lg:grid-cols-2",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
					className: "text-lg font-bold",
					children: "Booking Requests"
				}), /* @__PURE__ */ jsxs("div", {
					className: "mt-4 space-y-3",
					children: [
						loading && /* @__PURE__ */ jsx(Card, {
							className: "p-4",
							children: "Loading..."
						}),
						!loading && pending.length === 0 && /* @__PURE__ */ jsx(Card, {
							className: "p-4",
							children: "No pending requests."
						}),
						pending.map((b) => /* @__PURE__ */ jsxs(Card, {
							className: "p-4",
							children: [
								/* @__PURE__ */ jsxs("div", {
									className: "flex justify-between",
									children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
										className: "font-semibold",
										children: b.category
									}), /* @__PURE__ */ jsxs("div", {
										className: "text-xs text-muted-foreground",
										children: [
											b.date,
											" • ",
											b.time
										]
									})] }), /* @__PURE__ */ jsxs(Badge, { children: ["₹", b.price] })]
								}),
								/* @__PURE__ */ jsx("p", {
									className: "mt-3 text-sm text-muted-foreground",
									children: b.description
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "mt-4 flex gap-2",
									children: [/* @__PURE__ */ jsx(Button, {
										size: "sm",
										onClick: () => decide(b.id, "ACCEPTED"),
										children: "Accept"
									}), /* @__PURE__ */ jsx(Button, {
										size: "sm",
										variant: "outline",
										onClick: () => decide(b.id, "CANCELLED"),
										children: "Decline"
									})]
								})
							]
						}, b.id))
					]
				})] }), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
					className: "text-lg font-bold",
					children: "Today's Schedule"
				}), /* @__PURE__ */ jsxs("div", {
					className: "mt-4 space-y-3",
					children: [today.map((b) => /* @__PURE__ */ jsx(Card, {
						className: "p-4",
						children: /* @__PURE__ */ jsxs("div", {
							className: "flex justify-between",
							children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
								className: "font-semibold",
								children: b.category
							}), /* @__PURE__ */ jsxs("div", {
								className: "text-xs text-muted-foreground",
								children: [
									b.time,
									" • ",
									b.address
								]
							})] }), /* @__PURE__ */ jsx(Badge, { children: b.status })]
						})
					}, b.id)), !loading && today.length === 0 && /* @__PURE__ */ jsx(Card, {
						className: "p-4",
						children: "Nothing scheduled today."
					})]
				})] })]
			})
		]
	});
}
function StatCard({ label, value, icon: Icon }) {
	return /* @__PURE__ */ jsxs(Card, {
		className: "flex items-center gap-4 p-5",
		children: [/* @__PURE__ */ jsx("div", {
			className: "grid h-12 w-12 place-items-center rounded-xl bg-accent",
			children: /* @__PURE__ */ jsx(Icon, { className: "h-5 w-5" })
		}), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
			className: "text-2xl font-bold",
			children: value
		}), /* @__PURE__ */ jsx("div", {
			className: "text-xs uppercase tracking-wider text-muted-foreground",
			children: label
		})] })]
	});
}
//#endregion
export { WorkerDashboard as component };
